@page
@model GenerateRecoveryCodesModel
@{
    ViewData["Title"] = "Gerar cód de recuperação 2FA";
    ViewData["ActivePage"] = ManageNavPages.TwoFactorAuthentication;
}

<partial name="_StatusMessage" for="StatusMessage" />
<h3>@ViewData["Title"]</h3>
<div class="alert alert-warning" role="alert">
    <p>
        <span class="glyphicon glyphicon-warning-sign"></span>
        <strong>Put these codes in a safe place.</strong>
    </p>
    <p>
        Seu você perder acesso ao displositivo e não tiver acesso aos códigos de verificação, irá perder acesso a sua conta
    </p>
    <p>
        A geração de novos códigos de recuperação não altera as chaves usadas nos aplicativos autenticadores. Se você deseja alterar a chave
        usado em um aplicativo autenticador, você deve <a asp-page="./ResetAuthenticator">redefinir seus código de autenticação</a>
    </p>
</div>
<div>
    <form method="post">
        <button class="btn btn-danger" type="submit">Gerar código de recuperação</button>
    </form>
</div>
